services:
  - type: web
    name: review-insight-system
    env: python
    region: oregon
    plan: free
    buildCommand: bash build.sh
    startCommand: gunicorn api:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.11
